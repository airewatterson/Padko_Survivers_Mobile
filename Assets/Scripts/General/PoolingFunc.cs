using Enemy;
using UnityEngine;
using UnityEngine.Pool;

namespace General
{
    public class PoolingFunc : SingletonMonoBehavior<PoolingFunc>
    {
        //

        private ObjectPool<GameObject> _pool;

        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
